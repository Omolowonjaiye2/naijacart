import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Eye,EyeOff,Mail,Lock} from 'lucide-react';export default function Login(){const [show,setShow]=useState(false);const nav=useNavigate();return <main className="auth"><div className="auth-card"><Link to="/" className="logo"><span>NC</span> NaijaCart</Link><p className="eyebrow">WELCOME BACK</p><h1>Sign in to your account</h1><p className="auth-sub">Access orders, saved products and account settings.</p><form onSubmit={e=>{e.preventDefault();nav('/account')}}><label>Email address<div className="input-icon"><Mail/><input required type="email" placeholder="you@example.com"/></div></label><label>Password<div className="input-icon"><Lock/><input required type={show?'text':'password'} placeholder="••••••••"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><div className="remember"><label><input type="checkbox"/> Remember me</label><a href="#">Forgot password?</a></div><button className="primary full">Sign In</button></form><div className="or"><span>or continue with</span></div><div className="social-login"><button>Google</button><button>Apple</button></div><p className="auth-foot">New to NaijaCart? <Link to="/register">Create an account</Link></p><small className="demo-note">Demo UI only — no real authentication occurs.</small></div></main>}
