import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Eye,EyeOff} from 'lucide-react';export default function Register(){const [show,setShow]=useState(false);const nav=useNavigate();return <main className="auth"><div className="auth-card"><Link to="/" className="logo"><span>NC</span> NaijaCart</Link><p className="eyebrow">JOIN NAIJACART</p><h1>Create your account</h1><p className="auth-sub">Save favourites, track demo orders and shop faster.</p><form onSubmit={e=>{e.preventDefault();nav('/account')}}><label>Full name<input required placeholder="Your full name"/></label><label>Email address<input required type="email" placeholder="you@example.com"/></label><label>Password<div className="input-icon"><input required minLength="6" type={show?'text':'password'} placeholder="At least 6 characters"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><label>Confirm password<input required type="password" placeholder="Repeat password"/></label><button className="primary full">Create Account</button></form><div className="or"><span>or</span></div><div className="social-login"><button>Google</button><button>Apple</button></div><p className="auth-foot">Already have an account? <Link to="/login">Sign in</Link></p><small className="demo-note">Demo UI only — no real authentication occurs.</small></div></main>}
