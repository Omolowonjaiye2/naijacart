import {useEffect,useState} from 'react';import {useSearchParams,Link} from 'react-router-dom';import {searchProducts} from '../services/api';import ProductGrid from '../components/ProductGrid';import LoadingSkeleton from '../components/LoadingSkeleton';import {Search} from 'lucide-react';
export default function SearchResults({onToast}){const [sp]=useSearchParams();const q=sp.get('q')||'';const [products,setProducts]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(false);useEffect(()=>{setLoading(true);searchProducts(q).then(d=>setProducts(d.products||[])).catch(()=>setError(true)).finally(()=>setLoading(false))},[q]);return <main className="shop-page"><div className="container"><div className="breadcrumbs">Home / Search</div><div className="search-heading"><Search/><div><p className="eyebrow">SEARCH RESULTS</p><h1>Results for “{q}”</h1></div></div>{loading?<LoadingSkeleton/>:error?<div className="state"><h3>Unable to load products. Please try again.</h3></div>:products.length?<ProductGrid products={products} onToast={onToast}/>:<div className="state"><h3>No products found</h3><p>We couldn't find anything for “{q}”.</p><Link className="primary" to="/products">Browse products</Link></div>}</div></main>}
