import {Link,useNavigate} from 'react-router-dom'; import {Search,Heart,ShoppingBag,User,Menu,X,ChevronDown} from 'lucide-react'; import {useCart} from '../context/CartContext'; import {useWishlist} from '../context/WishlistContext'; import {useState} from 'react';
export default function Navbar(){const [q,setQ]=useState('');const [open,setOpen]=useState(false);const nav=useNavigate();const {count}=useCart();const {count:wcount}=useWishlist();const submit=e=>{e.preventDefault();if(q.trim())nav(`/search?q=${encodeURIComponent(q.trim())}`);};return <><div className="announcement">Free delivery on selected orders over ₦100,000 · Shop with confidence across Nigeria</div><header className="nav"><div className="nav-inner"><Link to="/" className="logo"><span>NC</span> NaijaCart</Link><Link className="cat-link" to="/products">Categories <ChevronDown size={15}/></Link><form className="search" onSubmit={submit}><Search size={19}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search phones, fashion, laptops and more..."/><button>Search</button></form><div className="nav-actions"><Link to="/account" title="Account"><User/><small>Account</small></Link><Link to="/wishlist" title="Wishlist" className="badge-wrap"><Heart/><small>Wishlist</small>{wcount>0&&<b>{wcount}</b>}</Link><Link to="/cart" title="Cart" className="badge-wrap"><ShoppingBag/><small>Cart</small>{count>0&&<b>{count}</b>}</Link><button className="menu-btn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<div className="mobile-menu"><form className="search" onSubmit={submit}><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..."/><button>Go</button></form><Link onClick={()=>setOpen(false)} to="/products">Shop All</Link><Link onClick={()=>setOpen(false)} to="/wishlist">Wishlist ({wcount})</Link><Link onClick={()=>setOpen(false)} to="/cart">Cart ({count})</Link><Link onClick={()=>setOpen(false)} to="/account">Account</Link></div>}</header></>}
