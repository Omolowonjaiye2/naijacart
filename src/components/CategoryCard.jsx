import {Link} from 'react-router-dom';import {ArrowUpRight} from 'lucide-react';export default function CategoryCard({category,i}){const label=category.name||category;return <Link to={`/products/${encodeURIComponent(category.slug||label)}`} className={`category-card cat-${i%6}`}><div><span>{String(i+1).padStart(2,'0')}</span><h3>{label.replaceAll('-',' ')}</h3><p>Explore collection</p></div><ArrowUpRight/></Link>}
